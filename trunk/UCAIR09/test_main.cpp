#include "test_main.h"
#include <iostream>
#include <map>

#include "bing_wrapper.h"

using namespace std;

namespace ucair {

void testMain() {
	// Put your adhoc test code here.

	/*BingWrapper search_engine;

	int start_pos = 1;
	int result_count = 10;

	Search search;
	search.query.text = "czhai";
	if (! search_engine.fetchResults(search, start_pos, result_count)){
		cerr << "failed" << endl;
		return;
	}
	for (map<int, SearchResult>::const_iterator itr = search.results.begin(); itr != search.results.end(); ++ itr) {
		cerr << itr->second.title << endl;
	}*/
}

}